enum SoundEffectId {
  buttonTap,
  pageTransition,
  incorrectAnswer,
  incorrectAnswerAlternative,
  encouragement,
  xpReward,
  dailyStreak,
  badgeUnlock,
  levelUp,
  mascotAppearance,
  mascotWave,
  audioLogo,
}

enum BackgroundMusicId { home, learning }

enum AudioCategory { ui, feedback, reward, mascot, brand }
